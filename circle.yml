dependencies:
  pre:
    - curl -v -L -o cf-cli_amd64.deb 'https://cli.run.pivotal.io/stable?release=debian64&source=github'
    - sudo dpkg -i cf-cli_amd64.deb
    - cf -v
test:
  post:
    - cf api  https://api.run.pivotal.io
    - cf auth $CF_USER_EMAIL $CF_PASSWORD
    - cf target -o proxy.gap.com -s development
    - cf set-env rabbitmq-email NEW_RELIC_LICENSE_KEY $NEW_RELIC_LICENSE_KEY
    - cf set-env rabbitmq-email NEW_RELIC_APP_NAME crawl-products-service
    - cf push crawl-products-service
    - cf create-service newrelic standard crawl-products-service-app
    - cf bind-service crawl-products-servicel rcrawl-products-service-app
    - cf restage crawl-products-service